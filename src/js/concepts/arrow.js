/**
 * Syntax
 * Arguments object
 * new keyword 
 * Duplicate params passing
 * Implicit return statement
 * 'this' keyword functionality
 */

let squareNum = (x) => x * x;

let sayHi = _ => console.log("Hi");

sayHi();
squareNum(10);