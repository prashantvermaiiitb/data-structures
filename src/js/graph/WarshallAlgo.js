/**
 * generating transitive closure graph for the given
 * adjacency matrix for the Directed acyclic graph.
 * e.g: if there is an edge from A -> B and B-> C then
 * there is an edge from A->C
 */
module.exports = function (graph) {
    let adjacencyMatrix = graph.getAdjacencyMatrix();
    for(let row = )
};
